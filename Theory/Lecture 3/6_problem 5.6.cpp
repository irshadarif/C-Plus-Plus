#include<iostream>
using namespace std;
main ()
{
	int n;
	cout<<"enter a number"<<endl;
	cin>>n;
	if (n>0)
		cout<<"the number is positive"<<endl;
	if (n<0)
		cout<<"the number is negative"<<endl;
	if(n==0)
		cout<<"the number is eqal to zero"<<endl;
	return 0;
}
