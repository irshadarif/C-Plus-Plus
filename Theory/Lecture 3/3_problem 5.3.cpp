#include<iostream>
using namespace std;
main ()
{
	int a, b;
	cout<<"enter a number"<<endl;
	cin>>a;
	cout<<"enter a number"<<endl;
	cin>>b;
	if (a*a==b)
		cout<<"the second number is square of first number";
	else
		cout<<"the second number is not the square of first number";
}
